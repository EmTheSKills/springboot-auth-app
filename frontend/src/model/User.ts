export type User = {
    id: string
    firstName: string
    lastName: string
    userName: string
    email: string
    phoneNumber: string
    profilePictureUrl: string
    registrationDate: string
    listPhotos: Array<string>
}
